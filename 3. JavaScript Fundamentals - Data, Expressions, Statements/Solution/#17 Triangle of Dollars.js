function triangleOfdollars(lineCount) {
    for (let line = 1; line <= lineCount; ++line) {
        console.log("$".repeat(line));
    }
}

triangleOfdollars(3);
triangleOfdollars(2);
triangleOfdollars(4);